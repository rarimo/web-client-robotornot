import { PageContainer, ValidatorSection } from '@/components'

const Validators = () => {
  return (
    <PageContainer>
      <ValidatorSection />
    </PageContainer>
  )
}

export default Validators
